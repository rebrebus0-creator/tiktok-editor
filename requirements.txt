# Ядро: транскрипция с таймкодами по словам.
# На Mac работает на CPU (CTranslate2), поэтому в config по умолчанию compute_type="int8".
faster-whisper>=1.1.0

# Тянется как зависимость faster-whisper, фиксируем явно —
# используется для чтения длительности/частоты кадров медиафайлов при необходимости.
av>=12.0.0

# ВНИМАНИЕ: FFmpeg ставится отдельно, это не Python-пакет:
#   brew install ffmpeg
# Проверить установку: python -m src.main check
